import { lang } from "./index";

export const INFORMATION = lang === "en" ? "INFORMATION" : "";
export const RANKING = lang === "en" ? "RANKING" : "";
export const TOPICS = lang === "en" ? "TOPICS" : "";
export const EVENTS = lang === "en" ? "EVENTS" : "";
export const PICK_UP = lang === "en" ? "PICK UP!" : "";
export const BIRTHDAY = lang === "en" ? "BIRTHDAY" : "";
export const SHOP_LIST = lang === "en" ? "SHOP LIST" : "";
export const CASTS = lang === "en" ? "CASTS" : "";

export const PHOTO = lang === "en" ? "PHOTO" : "";

export const VIDEO = lang === "en" ? "VIDEO" : "";

export const TWEET = lang === "en" ? "TWEET" : "";

export const BLOG = lang === "en" ? "BLOG" : "";

export const VIEW_IMAGE = lang === "en" ? "VIEW IMAGE" : "";

export const VIEW_BLOG = lang === "en" ? "VIEW BLOG" : "";

export const VIEW_TWEET = lang === "en" ? "VIEW TWEET" : "";

export const VIEW_MORE = lang === "en" ? "VIEW MORE..." : "";

export const HOST = lang === "en" ? "HOST" : "";

export const SHOP = lang === "en" ? "SHOP" : "";

export const FAVORITE = lang === "en" ? "FAVORITE" : "";

export const VIEW_DETAIL = lang === "en" ? "VIEW DETAIL" : "";

export const DAILY = lang === "en" ? "Daily" : "";

export const WEEK = lang === "en" ? "Week" : "";

export const MONTH = lang === "en" ? "Month" : "";

export const BIRTHDAY_OF_MONTH = lang === "en" ? "BIRTHDAY OF MONTH" : "";
export const BIRTHDAY_DETAIL = lang === "en" ? "BIRTHDAY DETAIL" : "";
export const BIRTHDAY_CARD = lang === "en" ? "BIRTHDAY CARD" : "";
export const NUMBER_OF_DONATE = lang === "en" ? "DONATED" : "";
export const DONATE_SETTINGS = lang === "en" ? "DONATE SETTINGS" : "";
export const MAKE_PRIVATED = lang === "en" ? "Privated" : "";

export const DONATE = lang === "en" ? "DONATE" : "";
export const GIFT_LIST = lang === "en" ? "GIFT LIST" : "";

export const SELECT_GIFT = lang === "en" ? "SELECT GIFT" : "";

export const FREE = lang === "en" ? "Free" : "";

export const YOU_HAVE_NOT_SELECT =
  lang === "en" ? "You have not selected a gift yet" : "まだギフトを選択していません";

export const PLEASE_ENTER_CONTENT =
  lang === "en" ? "Please enter content" : "コンテンツを入力してください";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";

// export const PHOTO = lang === 'en' ? "Photo" : "";
