const styles = (theme) => ({
  shopItem: {
    padding: 10,
    backgroundColor: "red",
    borderRadius: 10,
  },
  btn: {
    backgroundColor: "yellow",
    margin: 10,
    padding: 10,
    border: "10px",
  },
});

export default styles;
