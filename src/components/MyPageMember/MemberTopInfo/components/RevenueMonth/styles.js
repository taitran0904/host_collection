const styles = (theme) => ({
  wrapRevenue: {
    backgroundColor: "#ffff",
    paddingTop: 10,
  },

  titleRevenue: {
    fontSize: "1.2rem",
    fontWeight: "bold",
    alignItems: "center",
    dislay: "flex",
    justifyContent: "space-between",
    width: "100%",
  },

  groupRevenue: {
    fontSize: "1.1rem",

    "& p span": {
      fontWeight: "bold",
    },
  },
});

export default styles;
