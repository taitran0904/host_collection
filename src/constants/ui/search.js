import { lang } from "./index";

export const SEARCH = lang === "en" ? "SEARCH" : "";

export const SEARCH_FOR_HOST = lang === "en" ? "SEARCH FOR HOST" : "";

export const SEARCH_FOR_SHOP = lang === "en" ? "SEARCH FOR SHOP" : "";

export const PLACEHOLDER_HOST = lang === "en" ? "name, hashtag, ..." : "";

export const PLACEHOLDER_SHOP = lang === "en" ? "name, location, ..." : "";

export const ADVANCE_SEARCH = lang === "en" ? "Advance search" : "";

export const HEIGHT = lang === "en" ? "Height (less than)" : "";

export const BLOOD_TYPE = lang === "en" ? "Blood type" : "";

export const AGE = lang === "en" ? "Age (less than)" : "";

export const ZODIAC = lang === "en" ? "Zodiac" : "";

export const LARGE_LOCATION = lang === "en" ? "Large location" : "";

export const SMALL_LOCATION = lang === "en" ? "Small location" : "";

export const OPEN_TIME = lang === "en" ? "Open time" : "";
